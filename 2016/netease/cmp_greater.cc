/**
 * 比较两颗钻石谁更重
 * 使用链表将大小关系确定
 */
#include <iostream>
#include <algorithm>
//#include <list>
#include <vector>

using namespace std;

class Cmp{
    public:
        int cmp(int g1,int g2,vector<vector<int> > records,int n){
            int ret = 1;
            vector<vector<int> >::iterator p = records.begin();
            for(int i=1;i<n;++i){

            }
            vector<vector<int> >::iterator it;
        }
};

/**
 * 最大和子数组
 */

#include <iostream>
#include <algortihm>

using namespace std;

int maxChildArray(int a[],size_t sz,int &i,int &j)
{

}

int main(int argc, char *argv[])
{
    int a[]={1,2,-1,1,3,-2,4};
    size_t sz = sizeof(a)/sizeof(a[0]);
    int i,j;
    maxChildArray(a,sz,i,j);
    return 0;
}
